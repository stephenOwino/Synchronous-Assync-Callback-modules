const sum = (a, b) => a + b;

const sub = (a, b) => a - b;

/*another way of exporting files when dealing 
multiple functions,objects,variables instead of 
putting export keyword infront*/

//named export =we use the same name while importing
//default import and export

export { sum, sub };
